pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/Deymonryk/farm_breakdown.git'
            }
        }
        
	stage('Build') {
            steps {
                bat 'g++ -o myapp main.cpp'
            }
        }

        stage('Test') {
            steps {
                 bat 'echo "Run"'
            }
        }
        
    }
}
