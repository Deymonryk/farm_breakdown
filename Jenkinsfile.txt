pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/Deymonryk/farm_breakdown.git'
            }
        }
        
	stage('Build') {
            steps {
                bat 'mkdir build && cd build && cmake .. && make'
            }
        }

        stage('Test') {
            steps {
                bat 'cd build && ctest'
            }
        }
        
    }
}
